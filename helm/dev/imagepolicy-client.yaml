apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: platform-client
  namespace: saferworld
spec:
  imageRepositoryRef:
    name: platform-client
  filterTags:
    pattern: '^0\.\d+\.0-rc\.\w+$'
    extract: '$0'
  policy:
    semver:
      range: '>=0.0.1-0'
